box: lorentzca/rocketbox
build:
    steps:
        - script:
            name: pwd
            code: pwd
        - script:
            name: ls
            code: ls -R /
        - script:
            name: rkt help
            code: rkt --help
