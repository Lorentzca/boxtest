#box: lorentzca/rocketbox
box: wercker/default
build:
    steps:
        - script:
            name: pwd
            code: pwd
        - script:
            name: ls
            code: |
              ls -l /
              find / | grep rkt
        - script:
            name: rkt help
            code: rkt --help
