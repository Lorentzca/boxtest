box: wercker/default
services:
    - lorentzca/rocketbox
build:
    steps:
        - script:
            name: pwd
            code: pwd
        - script:
            name: rkt help
            code: rocket/rkt --help
