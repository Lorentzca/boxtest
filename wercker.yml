box: lorentzca/rocketbox
build:
    steps:
        - script:
            name: pwd
            code: pwd
        - script:
            name: ls
            code: |
              ls -l /
              find / | grep rkt
        - script:
            name: rkt help
            code: /etc/rkt --help
